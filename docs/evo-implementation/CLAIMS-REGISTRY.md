# Claims Registry

Every claim below was checked against the executable code, the database schema, or explicit demo-only labeling already present in the source. **A claim not listed here, or listed as "No", is not licensed for use in marketing copy in Phases 2–5 without new verification.**

Source-of-truth order used: (1) code/schema/routes, (2) verified existing repo content, (3) client handover docs, (4) screenshots, (5) outside research. No conflicts between code and handover docs were found for these claims, except where noted.

| # | Claim | Verified source / file | Safe to publish? | Audience | Notes / qualification |
|---|---|---|---|---|---|
| 1 | "Real login sessions backed by a real SQLite database" | `server.js` (`express-session`), `db.js` (`better-sqlite3`, WAL mode) | Yes | Internal/technical | Verified — not a mock/in-memory store. |
| 2 | Booking total is calculated from the school's actual calendar, never a flat guess | `lib/pricing.js` `calculateBookingTotal`, `migrations/004_school_calendar_remove_wallet.sql`, `tests/pricing.test.js` (10 passing cases incl. the "never zero" regression) | Yes | Parents, Schools | This is the core, verified fix for the "KWD 0.00" bug found in the live-system audit. |
| 3 | Civil ID is masked everywhere it is displayed, including in the edit form | `lib/mask.js`, `tests/civil-id-masking.test.js` (integration test asserts raw Civil ID never appears in `/profile`, `/students`, `/students/:id/edit` HTML) | Yes | Parents | Verified by an actual HTTP integration test, not just code inspection. |
| 4 | CSRF protection on every form | `csrf-sync` middleware in `server.js`, `_csrf` hidden field present in every POST form view checked | Yes | Internal/technical | Verified across all POST routes in the route contract. |
| 5 | Login is rate-limited | `express-rate-limit`, `loginLimiter` (10 attempts / 15 min / IP), applied to both `/login` and `/school-admin/login` | Yes | Internal/technical | Verified in code. |
| 6 | "A real wallet with transaction history" | ~~README (pre-fix)~~ | **No** | — | **False as of this audit.** `migrations/004_school_calendar_remove_wallet.sql` dropped the `wallets`/`wallet_transactions` tables; no `/wallet` route exists in `server.js`. README corrected in Phase 1. |
| 7 | Cancellation refunds the parent | `db.cancelAndRefund`, notification text: "refunded to your original payment method (demo — simulated)" | Yes, **only with the "demo — simulated" qualifier** | Parents | No real payment gateway is invoked. Must not be published without the simulation qualifier. |
| 8 | KNET payment processing | `server.js` `/booking` success message: "charged via KNET (demo — no real payment processed)"; `.env.example` shows `KNET_MERCHANT_ID`/`KNET_API_KEY` are blank pending sandbox credentials | Yes, **only with the "demo, no real payment" qualifier** | Parents | Do not publish as if live payment processing exists. |
| 9 | "Most parents choose this" (subscription plan badge) | No supporting data source exists (no analytics/selection-stats table in schema) | **No** | Parents | Unverified comparative/statistical claim. Not removed in Phase 1 (a copy decision), but flagged as **not licensed** until either real data backs it or it is reworded non-comparatively (e.g. "Recommended"). |
| 10 | Allergen-free / dietary claims (nut-free, shellfish-free, etc.) | `menu.ejs`, `home.ejs` FAQ — both carry explicit disclaimers: "not independently verified by Evo Meals", "per-dish information, not an independently verified guarantee" | Yes, **only together with the existing disclaimer copy** | Parents, Students | Must never be shortened to a blanket "allergen-free" claim without the disclaimer — this is an explicit guardrail in the transformation brief. |
| 11 | Portion sizes matched to student's grade | `home.ejs` copy: "Portioned for your child's age... every student's grade is on file" | Partially — **UI/data claim not enforced in code** | Parents | `students.class` is stored and displayed, but no code path in `server.js`/`db.js` was found that actually varies a *portion size* or *serving* by grade — menu items have one fixed `calories`/`protein`/etc. value each, not a per-grade variant. **This claim currently outruns the implementation.** Flagged in Blockers — do not expand this claim further until portion-by-grade logic actually exists, or reword to remove the implication of automatic portion adjustment. |
| 12 | School-admin dashboard shows "real-time data... not a preview" | `school-admin-dashboard.ejs`, backed by live `db.getStudentsBySchool`/`db.getBookingsForSchool` queries | Yes | Schools | Verified — genuinely queries current rows, not canned data, though the underlying rows are demo/seed data in this environment. |
| 13 | Homepage dashboard screenshot is explicitly a "preview... not live data from your program" | `home.ejs:468` | Yes | Schools (top-of-funnel) | Correct self-disclosure already in place — do not remove this qualifier in later phases. |
| 14 | Trust statistics (school count, students served, meals served, etc.) | None found — `SHOW_TRUST_STATS = false` in `home.ejs`, explicitly gated off with no data behind it | **No — do not turn on without real figures** | All | The existing gate (`SHOW_TRUST_STATS`) is the correct pattern; a later phase must supply real numbers before flipping it, never a placeholder number. |
| 15 | Testimonials (parent/school/caterer quotes) | None found — `SHOW_TESTIMONIALS = false`, placeholder text explicitly reads "[Quote pending — real testimonial from a parent using Evo Meals]" | **No — do not invent or turn on without real, attributable quotes** | All | Same pattern as #14. Do not attribute a quote to an invented person. |
| 16 | Company history / "wasn't built in a boardroom" narrative on `/about` | `about.ejs` — no specific founding date, funding, headcount, or award claims made; text is values-level ("our approach", "our focus", "our standard"), not factual-history claims | Yes, as currently written | All | Safe because it makes no falsifiable factual claims. Any future addition of a founding date, team size, or funding history must be independently verified first — none currently exists in the repo to verify against. |
| 17 | Location: Farwaniya, Kuwait; contact email `info@evo360.tech`; phone `+965 9957 0836`; app store links | `partials/sitefoot.ejs`, `partials/appfoot.ejs` | Not independently verifiable from the codebase | All | These are contact-details claims, not verifiable from source code alone (source of truth #3, client handover, would be required) — carried forward as-is since they pre-date this audit and were not flagged as incorrect by the client; **not independently confirmed as accurate in Phase 1**. |
| 18 | App Store / Google Play links point to a real published app | `sitefoot.ejs` links to `apps.apple.com/us/app/evo-school-meal/id6738001343` and `play.google.com/store/apps/details?id=com.pmg.school` | Not verified | Parents | Out of scope for a code audit — verifying a live app-store listing requires outside research explicitly authorized by the client; not performed in Phase 1. |
| 19 | Terms/Privacy "Last updated 19 September 2026" | `terms.ejs`, `privacy.ejs` | Yes (internally consistent) | Legal | Matches the repo's actual recent activity; not independently significant beyond being an honest, current date rather than a stale placeholder. |
| 20 | "Evo Meals connects parents with an independent catering partner — it does not prepare meals itself" | `sitefoot.ejs`, corroborated by `terms.ejs`'s marketplace/no-food-preparation language | Yes | All | Consistent across footer and legal text — a genuine structural claim about the business model, not a promotional one. |

**Anything not on this list is unverified by this audit and must not be treated as publishable fact.**
